[package]
name = "blindroute-core"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Unified FHE traits, arithmetic circuit IR, and shared types for BlindRoute"
repository.workspace = true
readme.workspace = true

[lib]
crate-type = ["rlib"]

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
